import 'package:flutter/material.dart';

class ArticleFormCreationWidget extends StatelessWidget {
  const ArticleFormCreationWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}